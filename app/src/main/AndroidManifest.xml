<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gnusl.wow">

    <application
        android:name=".Application.WowApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_wow"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_wow"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.SplashActivity"
            android:configChanges="orientation"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBarSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.LoginActivity"
            android:configChanges="orientation"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBarSplash" />
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBarMain"></activity>
        <activity
            android:name=".Activities.SearchActivity"
            android:theme="@style/AppTheme.NoActionBarSearch"></activity>

        <activity
            android:name=".Activities.RoomChatActivity"
            android:configChanges="orientation"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBarChatRoom">

        </activity>

    </application>

</manifest>